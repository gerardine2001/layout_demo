
dart
import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Education App',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: EducationApp(),
    );
  }
}

class EducationApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Education App'),
      ),
      body: Center(
        child: Text(
          'Welcome to Education App!',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}


This is a basic Flutter app that displays a text "Welcome to Education App!" on the screen. You can customize and expand this app by adding more features such as courses, lessons, quizzes, etc.